{
  "name": "railway-booking-backend",
  "version": "1.0.0",
  "description": "Express backend for railway booking system with MongoDB",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon --exec \"node --experimental-specifier-resolution=node\" src/server.js",
    "build": "echo \"No build step needed for JS backend\"",
    "start": "node --experimental-specifier-resolution=node src/server.js",
    "seed": "node --experimental-specifier-resolution=node src/seed.js",
    "test": "jest --coverage"
  },
  "keywords": [
    "railway",
    "booking",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.7.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
